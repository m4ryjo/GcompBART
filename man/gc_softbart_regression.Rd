% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gc_softbart_regression.R
\name{gc_softbart_regression}
\alias{gc_softbart_regression}
\title{SoftBART Regression}
\usage{
gc_softbart_regression(
  X,
  Y,
  X_test = NULL,
  num_tree = 20,
  k = 2,
  hypers = NULL,
  opts = NULL,
  verbose = TRUE
)
}
\arguments{
\item{X}{A numeric matrix of predictors for training.}

\item{Y}{A numeric vector of outcomes for training.}

\item{X_test}{Optional numeric matrix of predictors for testing.}

\item{num_tree}{Number of trees in the ensemble (default = 20).}

\item{k}{Prior parameter controlling shrinkage (default = 2).}

\item{hypers}{Optional list of hyperparameters (created by `Hypers()`).}

\item{opts}{Optional list of options (created by `Opts()`).}

\item{verbose}{Logical; if TRUE, prints progress information.}
}
\value{
A list containing:
\describe{
  \item{mu_train_mean}{Posterior mean predictions for training data.}
  \item{mu_test_mean}{Posterior mean predictions for test data (if provided).}
  \item{mu_Y}{Training outcome mean.}
  \item{sd_Y}{Training outcome standard deviation.}
  \item{ecdfs}{List of normalization functions for predictors.}
  \item{opts}{Options used in the model.}
  \item{forest}{The fitted Forest object.}
}
}
\description{
Fits a Bayesian Additive Regression Trees (SoftBART) model for continuous outcomes.
}
\examples{
\dontrun{
X <- matrix(rnorm(200), nrow = 20)
Y <- rnorm(20)
fit <- gc_softbart_regression(X, Y, num_tree = 50)
str(fit)
}
}
