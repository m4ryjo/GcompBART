% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gc_softbart_probit.R
\name{gc_softbart_probit}
\alias{gc_softbart_probit}
\title{SoftBART Probit Model}
\usage{
gc_softbart_probit(
  X,
  Y,
  X_test = NULL,
  num_tree = 20,
  k = 1,
  hypers = NULL,
  opts = NULL,
  verbose = TRUE
)
}
\arguments{
\item{X}{A numeric matrix of predictors for training.}

\item{Y}{A binary outcome vector (0/1 or factor with two levels).}

\item{X_test}{Optional numeric matrix of predictors for testing.}

\item{num_tree}{Number of trees in the ensemble (default = 20).}

\item{k}{Prior parameter controlling shrinkage (default = 1).}

\item{hypers}{Optional list of hyperparameters (created by `Hypers()`).}

\item{opts}{Optional list of options (created by `Opts()`).}

\item{verbose}{Logical; if TRUE, prints progress information.}
}
\value{
A list containing:
\describe{
  \item{mu_train_mean}{Posterior mean predictions for training data (latent scale).}
  \item{mu_test_mean}{Posterior mean predictions for test data (latent scale, if provided).}
  \item{offset}{Probit offset based on observed proportion.}
  \item{ecdfs}{List of normalization functions for predictors.}
  \item{opts}{Options used in the model.}
  \item{forest}{The fitted Forest object.}
}
}
\description{
Fits a Bayesian Additive Regression Trees (SoftBART) model for binary outcomes using a probit link.
}
\examples{
\dontrun{
X <- matrix(rnorm(200), nrow = 20)
Y <- sample(c(0, 1), size = 20, replace = TRUE)
fit <- gc_softbart_probit(X, Y, num_tree = 50)
str(fit)
}
}
